#include <stdio.h>

int main()
{
    char character;

    printf("Enter a character: ");
    scanf(" %c", &character);

    printf("The number equivalent is: %d\n", (unsigned char)character);

    return 0;
}
